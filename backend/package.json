{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "description": "Convex backend for the rental platform.",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "codegen": "convex codegen",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "convex": "^1.39.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
